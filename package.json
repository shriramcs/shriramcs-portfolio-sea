{
  "name": "portfolio-sea",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name=portfolio-sea"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "wrangler": "^3.78.0"
  }
}

